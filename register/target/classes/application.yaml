server:
  port: 8081
spring:
  application:
    name: register
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/register
    username: emildobrev
  secret: kTK1B6V1E4klYEnfjSDOepThdKcb0est4LK3HZLOsao
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  rabbitmq:
    addresses: localhost:5672
  profiles:
    active: default
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

